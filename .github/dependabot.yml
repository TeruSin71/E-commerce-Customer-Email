version: 2
updates:
  # Keep npm dependencies patched (security + version bumps).
  - package-ecosystem: npm
    directory: "/"
    schedule:
      interval: weekly
    open-pull-requests-limit: 5

  # Keep the GitHub Actions themselves patched.
  - package-ecosystem: github-actions
    directory: "/"
    schedule:
      interval: weekly
